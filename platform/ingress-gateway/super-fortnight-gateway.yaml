apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: super-fortnight-gateway
  namespace: istio-system
spec:
  selector:
    app: istio-ingress
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*.api.local"
